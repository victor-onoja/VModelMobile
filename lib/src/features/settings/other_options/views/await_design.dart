import 'package:vmodel/src/vmodel.dart';

class AwaitDesignScreen extends StatelessWidget {
  const AwaitDesignScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: const Text('Awaiting Design Updates'),
    );
  }
}
